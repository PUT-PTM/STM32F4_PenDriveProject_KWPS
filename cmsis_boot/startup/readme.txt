This directory contains startup routine:

startup_stm32f4xx.s :		contains startup routine for STM32F4XX processors
							setting up basic system functions (i.e. clock
							source), defining stack and heap area setup,
							defining vector table and eventually calling main()
